[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404

[build]
  publish = "public"
  command = "export NODE_OPTIONS=--max_old_space_size=4096 && npm install && hexo clean && hexo generate --debug"

[build.environment]
  NODE_VERSION = "18"

[build.processing]
  skip_processing = false